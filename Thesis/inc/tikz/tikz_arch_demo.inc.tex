% !TeX root = ../thesis_main copy.tex
\begin{figure}[!h]
    \centering
    \tikzstyle{block} = [rectangle, draw, fill=green!80!blue!70,
    text width=5em, text centered, rounded corners, minimum height=4em]
    \tikzstyle{line} = [draw, very thick, color=black!50, -latex']

    \begin{tikzpicture}[
        align=center,
        scale=0.2,
        node distance=3.5cm,
        auto]

        % Frontend
        \node [block] (webapp) {\includegraphics[width=.66\textwidth]{fig/vue.png}\\WebApp};

        % Microservice Cluster
        \node [block, right of=webapp, xshift=15mm] (authbackend) {\includegraphics[width=.65\textwidth]{fig/python-logo.png}\\Backend};

        % DB Stuff
        \node[database,label=below:SQL DB,database radius=.8cm,database segment height=0.46cm, right of=authbackend, xshift=15mm] (userdb) {};

        % DEMO
        \node [below of=authbackend, yshift=-1cm] (demo) {\fontfamily{lmss}\selectfont\Huge \color{uos-red-full}{{\fontsize{38}{50}\selectfont DEMO}}};

        % Edges & Paths
        \path [line] (webapp) -- node [text width=3.5cm, align=center, above]{GraphQL\\ request to} (authbackend);
        \path [line] (authbackend) -- node [text width=2.4cm, align=center, above]{Fetches\\ data from} (userdb);

    \end{tikzpicture}
    \caption{DemoService Architecture}\label{fig::arch_demo}
\end{figure}

